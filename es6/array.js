const arr = [9, 8, 7, 6];

// copyWithin方法
// arr.copyWithin(1,0,1)
// console.log(arr);

// find 和 findIndex两个方法
// console.log(arr.find((s) => s == 111));
// console.log(arr.findIndex((s) => s == 111));

// entries 和 keys values方法
// const entries = arr.entries();
// const keys = arr.keys();
// const values = arr.values();
// // eslint-disable-next-line no-restricted-syntax
// for (const value of entries) {
//   console.log(value);
// }
// // eslint-disable-next-line no-restricted-syntax
// for (const value of keys) {
//   console.log(value);
// }
// // eslint-disable-next-line no-restricted-syntax
// for (const value of values) {
//   console.log(value);
// }
console.log(arr.at(0));
